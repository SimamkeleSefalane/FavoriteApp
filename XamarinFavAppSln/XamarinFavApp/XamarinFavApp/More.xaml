<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinFavApp.More" Title="More" BackgroundColor="" IconImageSource="more.png">
    <ContentPage.Content>
        <StackLayout>

            <ListView ItemsSource="{Binding MoreItems}" >
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="xamarinfavapp:MoreItems">

                        <ViewCell>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Grid.ColumnDefinitions>

                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Label Padding=" 10,5,10,5" Text="{Binding MoreName}" FontSize="16" FontAttributes="Bold" TextColor="Black"  Grid.Column="0"  Grid.Row="0"/>

                            </Grid>

                        </ViewCell>

                    </DataTemplate>



                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>